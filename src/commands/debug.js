module.exports = {
    /*
    name: 'debug',
    description: 'Retorna os argumentos enviado, uso de teste e debug',
    guildOnly: false,
    adminOnly: true,
    args: true,
    usage: '<arg1> <arg2> ...',
    execute(message, args) {
        if(args[0] === 'foo') 
            return message.channel.send('bar');
        
		
        message.channel.send(`Argumentos: ${args}\nTamanho dos Argumentos: ${args.length}`);
    },
    */
};